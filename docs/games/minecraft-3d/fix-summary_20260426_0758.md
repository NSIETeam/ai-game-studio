# MiniCraft 3D Fix Summary

**Objective**: Apply 7 fixes from REVIEW.md to `index.html`, prioritizing must-fix bugs.

**Changes applied to**: `C:\Users\KING\.qclaw\workspace\ai-game-studio\games\minecraft-3d\index.html` (19.3 KB, under 25KB limit)

## 🔴 Must-Fix (4/4)

| # | Issue | Fix | Status |
|---|-------|-----|--------|
| 1 | Leaves can't be destroyed (`hitBlock()` skips `leaves` type) | Removed `if(b.type==='leaves')continue;` line | ✅ |
| 2 | Leaves not registered in `BLOCKS` / `BLOCK_LIST` | Added `leaves:{name:'树叶', tex:['leaves',...]}` to `BLOCKS`; added `'leaves'` to `BLOCK_LIST` | ✅ |
| 3 | GPU memory leak in `Chunk.rebuild()` | Added `geometry.dispose()` + `material.dispose()` before replacing old mesh in both solid and transparent paths | ✅ |
| 4 | First jump fails (`onGround` starts `false`) | Changed `let onGround=false;` → `let onGround=true;` | ✅ |

## 🟡 Suggested-Fix (3/3)

| # | Issue | Fix | Status |
|---|-------|-----|--------|
| 5 | World height H=6 conflicts with tree top y=7 | Changed `const H=6;` → `const H=7;` | ✅ |
| 6 | Space key scrolls page | Changed `if(e.code==='Space'&&pointerLocked)` → `if(e.code==='Space')` for unconditional `preventDefault()` | ✅ |
| 7 | Hot-reload textures may not display | Added `tex.needsUpdate=true` to all 4 texture-returning paths in `makeTex()` and IIFE texture generators | ✅ |

**File size**: 19,737 bytes (well under 25KB limit)
**Total edits**: 10 targeted replacements

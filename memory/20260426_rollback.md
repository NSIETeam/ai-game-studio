# Rollback to v1.2 Single-Page Layout (2026-04-26)

## Objective
User reported games/pipeline sections appearing empty after multi-view refactor. Requested rollback to version without paging/module-cards.

## What Was Done
- Restored `src/App.vue` and `style.css` to commit `4294afa` (v1.2 — simple single-page layout: Hero → Pipeline → Works → Capabilities → CTA → Footer, no `setView`/`activeView` paging)
- Added MiniCraft 3D to `realWorks` array (4th game entry)
- Added Finance Legion link in navigation
- Fixed double-commas syntax errors from previous manual edits
- Built, committed (bf051b1), pushed to GitHub Pages
- **Verified**: Deployed successfully, homepage responds 200 OK

## Key Decisions
- Used `git show 4294afa:src/App.vue` binary pipe to restore clean source (workaround for UTF-16 PowerShell redirect bug)
- Replaced `realWorks` array via exact character-range string replacement (positions 9653–10874)
- Kept original style.css from v1.2 intact

## Outcome
- Site is back to functional single-page layout
- 4 playable game cards on home page (including MiniCraft 3D)
- Removed: `modules-grid`, `module-card`, `Discussion/About` views, `setView()` navigation
